{
    "builds": [
        {
            "src": "flight-fare-prediction/app.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "flight-fare-prediction/app.py"
        }
    ]
}
